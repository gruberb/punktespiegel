{
  "name": "punktespiegel-fantasy-dashboard",
  "version": "1.3.0",
  "type": "module",
  "workspaces": ["frontend"],
  "engines": { "node": ">=22" },
  "scripts": {
    "generate:recommendations": "node --experimental-strip-types scripts/generate-manager-recommendations.ts",
    "dev": "npm run generate:recommendations && npm run dev --workspace frontend",
    "build": "npm run generate:recommendations && npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspace frontend"
  }
}
